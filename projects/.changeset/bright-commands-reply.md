---
"@neontechspace/fluxerly": minor
---

Add atomic keyed command batches with per-command argument inference, plus command-context reply helpers that bind the incoming message and handler cancellation
