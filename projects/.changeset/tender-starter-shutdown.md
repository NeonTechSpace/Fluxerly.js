---
"@neontechspace/fluxerly": patch
---

Treat supervisor-initiated native client closure as normal when stopping a bot, while preserving run and cleanup failures

Keep non-Error failures such as NaN in the default JavaScript starter, including when cleanup also fails
