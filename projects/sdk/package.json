{
    "name": "@neontechspace/fluxerly",
    "version": "0.0.0",
    "private": true,
    "type": "module",
    "license": "Apache-2.0",
    "imports": {
        "#sdk/*": {
            "fluxerly-source": "./src/*.ts",
            "default": "./dist/*.js"
        }
    },
    "exports": {
        ".": {
            "types": "./dist/index.d.ts",
            "import": "./dist/index.js"
        },
        "./effect": {
            "types": "./dist/effect.d.ts",
            "import": "./dist/effect.js"
        }
    },
    "files": [
        "dist",
        "src"
    ],
    "scripts": {
        "format": "prettier --write .",
        "format:check": "prettier --check .",
        "build": "tsc -p tsconfig.json",
        "test": "vitest run",
        "test:types": "tsc -p tsconfig.test.json",
        "test:package": "node tests/package-check.mjs",
        "test:live": "node tests/live/sandbox.mjs",
        "test:live:sdk": "pnpm run build && node tests/live/sdk.mjs default && node tests/live/sdk.mjs effect",
        "test:live:messages": "pnpm run build && node tests/live/messages.mjs default && node tests/live/messages.mjs effect",
        "test:live:management": "pnpm run build && node tests/live/messages.mjs default --manage && node tests/live/messages.mjs effect --manage",
        "test:live:events": "pnpm run build && node tests/live/messages.mjs default --events && node tests/live/messages.mjs effect --events",
        "test:live:history": "pnpm run build && node tests/live/messages.mjs default --history && node tests/live/messages.mjs effect --history",
        "test:live:recovery": "pnpm run build && node tests/live/messages.mjs default --recover && node tests/live/messages.mjs effect --recover",
        "check": "pnpm run format:check && pnpm run build && pnpm run test:types && pnpm run test && pnpm run test:package"
    },
    "dependencies": {
        "effect": "4.0.0-rc.112",
        "neverthrow": "8.2.0",
        "ws": "8.21.3"
    },
    "devDependencies": {
        "@types/node": "24.13.3",
        "@types/ws": "8.18.1",
        "prettier": "3.9.6",
        "typescript": "7.0.2",
        "vitest": "5.0.0"
    }
}
